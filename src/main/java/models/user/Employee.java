package models.user;

public class Employee extends User {

    public Employee(int userID, String username, String password, int userType) {
        super(userID, username, password, userType);
    }
}
